{
  "name": "deepfake-tracker-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "axios": "^1.6.2",              // For making API requests to the backend
    "react": "^18.0.0",             // Core React library
    "react-dom": "^18.0.0",         // React DOM rendering
    "react-scripts": "5.0.1"        // React build scripts
  },
  "devDependencies": {
    "eslint": "^8.0.0"             // For catching code issues (optional)
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
